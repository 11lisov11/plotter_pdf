# Codex Project Memory: plotter_pdf

This file is a compact working memory distilled from the restored Codex histories for `D:\plotter_pdf`.

## Core Project

- Current project root: `D:\plotter_pdf`.
- Main workflow is CLI/headless, not restored PySide UI.
- Entry points:
  - `main.py`
  - `src/plotter_pdf_drawer.py`
  - `src/send_grbl_file.py`
  - `plotter_studio/core/protocol.py`
  - `plotter_studio/core/serial_worker.py`
- Do not restore the old UI unless the user explicitly asks for UI code. The prior confusion was that the user wanted Codex chat history, not UI.
- There is a dirty worktree with many user/generated changes. Do not revert unrelated files.

## Machine Rules

- Default wired port: `COM6`.
- Common Bluetooth port: `COM11`.
- Baud: `115200`.
- Controller: GRBL/CH340 USB serial.
- Pen lift method: Z axis only, not `M3/M5`, not `M280`.
- Canonical constants from `config/PLOTTER_CONTROL_RULES.md`:
  - `Z_UP = 0.0`
  - `Z_DOWN = 11.9`
  - work area `X: 0..180`
  - work area `Y: -280..0`
  - production offset `WORK_OFFSET_Y_MM = -5.0`
- Direction lock:
  - `X+` right
  - `X-` left
  - `Y+` down
  - `Y-` up
- Origin:
  - `0,0` is left-lower corner of the inner work area.
  - After manually placing carriage there, run `G92 X0 Y0` and `G92 Z0`.
  - `G92` changes coordinates only; it does not move the machine.
- Preamble before real drawing:
  - `$X`
  - `$1=255`
  - `G21`
  - `G90`
  - `G92 Z0`
  - `G92 X0 Y0` after physical placement
- End jobs with `M5`, `$1=0`, optionally `$SLP` to release/cool motors.

## Calibration And Drawing

- Corner calibration command:
  - `python src\plotter_pdf_drawer.py --calibrate-corners`
  - or with explicit port: `python main.py --calibrate-corners --com COM6`
- Work frame uses shifted production window:
  - bottom `Y=-5`
  - top `Y=-285`
  - right `X=180`
  - left `X=0`
- The current session detected USB on `COM6` and got `<Idle...>` response. Do not assume calibration finished unless the user confirms; the chat restoration interrupted it.

## A3 Two-Pass Rule

- A3 is split as `2 columns x 1 row`.
- `pass_01`: normal orientation.
- `pass_02`: user physically rotates the sheet by `180 deg`.
- Backend must also rotate `pass_02` geometry by `180 deg` around the active area center before clipping/G-code.
- Then apply Y translation to already rotated geometry. Current rules file says `Y +3.0 mm`; README/history mention old `Y +4.0 mm`, so trust `config/PLOTTER_CONTROL_RULES.md` unless code/tests prove otherwise.
- Do not replace this with only horizontal or vertical mirroring.
- Important historical fix: A3 Method3 hybrid canvas keeps inner drawing `1:1`; do not reintroduce whole-page shrink-to-work-area for A3.

## Handwriting And Text

- Main user requirement: Russian text must become vector handwritten text, not printed text and not raster.
- For Cyrillic paragraph/body text, the intended route is TTF single-line/centerline with handwriting font, usually `Marck Script`.
- For technical drawings, formulas, dimensions, title blocks, and short numeric tokens, prefer technical/direct-vector or print-font routes for readability. Do not force everything through handwriting.
- Key failure previously found:
  - Cyrillic fell into TTF-to-raster-to-skeleton routes with fragmented strokes.
  - `--handwriting-direct-vector` did not really control routing.
  - mojibake like `Å`, `Ç` needed cp1251 repair.
  - not all `<text>` nodes were replaced, causing mixed handwritten/printed output.
  - Cyrillic join/smooth was disabled in places, causing broken words.
- Important concept: font-first routing for TOE/text-heavy pages. Page-level raster fallback is last resort, not default.
- Target behavior:
  - body text -> handwritten vector
  - formulas/technical labels/table text -> readable printed/vector route
  - diagrams/tables/line art -> exact vector geometry
  - image-only content -> traced only when needed

## Technical Drawing Optimization

- Do not break exact geometry, frames, dimensions, hatching, clipping, A3 pass alignment, or title-block placement.
- Existing improvement plan targets:
  - reduce pen lifts in text-heavy drawings by at least 35%
  - reduce tiny segments under `0.35 mm` by at least 50%
  - avoid dotted/point-like symbols in title blocks/dimensions
  - layout similarity must not regress more than `0.005`
- Avoid handwriting heuristics on technical drawings unless text is explicitly body handwriting content.
- `stitch` previously caused unwanted connecting lines in technical/A3 packages; preserve source order where needed.

## Testing Habit

- For code changes, run focused tests first, then broader pytest when geometry/backend is touched.
- Historical successful full runs were around `277/278 passed, 1 skipped, 1 deselected`.
- Useful checks:
  - `python -m py_compile src\plotter_pdf_drawer.py`
  - `python -m pytest -q`
  - targeted tests under `tests/`
  - package comparison artifacts: `report.json`, `summary.csv`, `source_vs_gcode_compare.*`, logs.
- For real hardware actions, explicitly state COM port, file, line count, elapsed time, and release motors at the end.

## Chat Restoration

- Restored history came from `D:\Users\11lis\.codex`.
- Current restored plotter threads are attached to `D:\plotter_pdf`.
- Rollout digest generated at `.codex_plotter_history_digest.md` for deeper local reference.

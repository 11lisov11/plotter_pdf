pyserial>=3.5
numpy>=2.0
Pillow>=10.4
PyMuPDF>=1.24

# Optional feature groups are defined in pyproject.toml:
#   pip install -e ".[photo,word,kompas,gui,dev]"
# Kept here for backward-compatible quick setup of the common Windows stack.
opencv-python-headless>=4.10
hershey-fonts>=2.1.0
pywin32>=306; platform_system == "Windows"
